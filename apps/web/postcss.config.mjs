// Configuración de PostCSS para Tailwind CSS v4
// En v4 ya no se usa tailwind.config.ts — la configuración va en globals.css
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
